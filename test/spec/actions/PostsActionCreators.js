'use strict';

describe('PostsActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/PostsActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
