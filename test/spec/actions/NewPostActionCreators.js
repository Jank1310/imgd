'use strict';

describe('NewPostActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/NewPostActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
