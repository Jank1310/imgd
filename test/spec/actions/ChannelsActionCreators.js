'use strict';

describe('ChannelsActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/ChannelsActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
