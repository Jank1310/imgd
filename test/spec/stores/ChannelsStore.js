'use strict';

describe('ChannelsStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/ChannelsStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
