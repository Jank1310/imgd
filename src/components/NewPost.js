'use strict';

var React = require('react/addons');

//var Actions = require('actions/xxx')

require('styles/AddPost.scss');

var AddPost = React.createClass({

  render: function () {
    return (
      <h1>New post</h1>
      );
  }
});

module.exports = AddPost;
